# Transfer Window 仓库协作要求

本文件适用于整个仓库。开始修改前先阅读 `README.md`、相关设计/路线图文档以及待修改模块；更深目录若存在自己的 `AGENTS.md`，则同时遵守更具体的要求。

## 基础要求

- 只修改完成当前任务所需的内容，保留用户已有和任务无关的改动；按下述提交要求提交，但不擅自打标签、发布或删除文件。
- Rust 代码负责确定性的领域事实、模拟和存档逻辑；React/TypeScript 负责界面与可视化；Tauri 只承担桌面边界。新增依赖或跨层调用前先确认没有破坏现有边界。
- 代码应清晰、类型安全并与周边风格一致。修复缺陷时尽可能补充回归测试；新增行为时补充对应测试或说明无法自动测试的原因。
- 更新行为、接口、数据格式、操作方式或验收结论时，同步更新相关文档。第三方素材或数据必须记录来源、许可证和署名要求，不得提交密钥、凭据或本机配置。
- 完成修改后运行与改动范围相称的检查。常用完整检查为 `cargo test --workspace`、`npm run test`、`npm run lint`、`npm run build` 和 `npm run docs:check`；只改文档时至少检查链接、格式和差异内容。
- 交付前检查 `git diff` 和 `git status`，确认没有生成物、调试代码或无关改动，并在结果中说明变更、版本处理以及实际执行的检查。

## 提交要求

- 每次完成足以需要重新跑通验证流程的修改后，在相关验证全部通过之后创建一次 Git commit。此类修改包括代码、依赖、构建或运行配置、数据格式、自动化流程、用户可感知行为，以及跨多个文件的实质性文档调整。
- 用户明确要求不要 commit 时不得提交。改动实在太小且无需重新跑完整验证流程时可以不单独提交，例如单个错别字、纯措辞或注释微调；这类改动应留待下一次相关提交一并纳入，或按用户要求单独提交。
- 验证失败时不得提交存在已知失败的改动；先修复并重新验证，无法修复则报告阻塞和未提交状态。
- 提交前检查 `git diff`、`git status` 和暂存内容，只纳入当前任务相关文件。提交信息应简洁说明结果，优先使用 Conventional Commits 风格。
- 提交后在交付结果中提供 commit 短哈希。除非用户明确要求，不执行 amend、push、创建 tag 或发布。

## 版本与更新日志

项目采用语义化版本 `X.Y.Z`。`package.json` 是当前版本的机器可读入口，且必须与 `CHANGELOG.md` 中最新的已发布版本一致。版本文件由 `scripts/version.mjs` 统一维护，禁止逐个手工修改。

每次完成更新后、结束任务前都必须：

1. 判断本次变更属于大版本、中版本还是小版本。
2. 更新 `CHANGELOG.md`；任何代码、配置、文档、素材或流程变更都不能遗漏。
3. 按下述策略升级版本号，或将小更新加入暂存区。
4. 运行 `npm run version:check`，检查所有版本声明、锁文件和文档是否保持一致。

### 升级判定

- **大版本（`X+1.0.0`）**：存在不兼容的公共接口、存档/数据格式、用户工作流变化，或产品方向发生根本调整。确认此类变更后立即升级。
- **中版本（`X.Y+1.0`）**：新增向后兼容、用户可感知的完整功能或显著能力。功能完成后立即升级。
- **小版本（`X.Y.Z+1`）**：向后兼容的缺陷修复、性能/体验微调、文档、测试、构建、素材或内部重构。此类更新默认先暂存，不逐条升级版本。
- 若一个任务包含多个级别，按最高级别处理。无法明确判断时采用较高一级，并在更新日志中写明影响。

### 小版本暂存规则

- 在 `CHANGELOG.md` 的“未发布”下维护“**小版本暂存区（N/5）**”。每个独立完成的小更新写一条面向用户的摘要；同一任务涉及多个文件仍计为一个小更新。
- 暂存达到 **5 个**独立小更新时，将补丁位 `Z` 增加 1，把全部暂存条目移入带当天日期的新版本节，并将暂存区重置为 `0/5`。
- 严重缺陷修复、安全修复、明确的发布要求或维护者决定可以在未满 5 条时提前发布小版本。
- 若暂存期间先发生大版本或中版本升级，把已有暂存条目一并纳入该版本，不再额外升级一次小版本，然后清空暂存区。

### 更新日志格式

- 未发布内容始终置顶；已发布版本使用 `## [vX.Y.Z] - YYYY-MM-DD`，日期采用本地当天日期。
- 按“新增、变更、修复、文档、维护”等实际需要分类，描述结果和影响，不堆砌文件名或实现步骤。
- 大/中版本立即建立发布节；小更新只写入暂存区，达到阈值或提前发布时才建立发布节。
- 不编造 Git 提交号、标签、发布日期或测试结果；提交号可在真实提交存在后补充。

### 版本同步位置

形成新版本时使用 GitHub Actions 的 `version` 工作流选择 `major`、`minor` 或 `patch`。工作流会更新 changelog、同步下列文件并创建 pull request；小版本未达到 5 条时会失败，只有经批准提前发布时才启用 `force_patch`。

需要在本地执行时，使用 `npm run version:bump -- patch|minor|major`；仅修复已有版本漂移时使用 `npm run version:set -- X.Y.Z`。完成后必须运行 `npm run version:check`。自动化覆盖：

- `package.json` 与 `package-lock.json` 的项目版本；
- 根 `Cargo.toml` 的 `workspace.package.version` 及根 `Cargo.lock` 中的 workspace 包版本；
- `src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json` 与 `src-tauri/Cargo.lock`；
- `CHANGELOG.md` 的版本标题，以及其他实际展示或引用版本号的文件。

路线图中的 `alpha-v0.1` 等里程碑、历史 changelog 标题、存档格式标识和依赖包自身版本不是当前项目版本，不随版本同步批量替换。除非任务明确要求，不创建 Git tag 或发布产物。
